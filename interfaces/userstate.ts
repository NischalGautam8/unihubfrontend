export interface userstate {
  user: {
    value: {
      username: string;
      acess_token: string;
      userid: string;
      firstName: string;
      lastName: string;
      refresh_token: string;
    };
  };
}
